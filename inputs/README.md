# Inputs

A convenient place for PDFs reviewed with `--pdf`. Not required: any of the
three modes accepts an absolute path anywhere, and `--build` and `--source`
take a LaTeX root that usually lives in the manuscript's own repository.

The contents of this directory are ignored by Git so private papers are not
committed. Keep only this README tracked.
